name: 'production_analytics'
version: '1.0.0'
config-version: 2

profile: 'production_analytics'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
clean-targets: ["target", "dbt_packages"]

models:
  production_analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table
